trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: SSH@0
  inputs:
    sshEndpoint: 'azure-vm-ssh'
    runOptions: 'inline'
    inline: |
      cd /home/azureuser/devops-webapp && \
      pwd && \
      git status
    readyTimeout: '20000'