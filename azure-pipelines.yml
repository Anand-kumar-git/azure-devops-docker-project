trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: SSH@0
  inputs:
    sshEndpoint: 'azure-vm-ssh'
    runOptions: 'inline'
    inline: |
      echo "Connected successfully"
      hostname
      pwd
      docker --version